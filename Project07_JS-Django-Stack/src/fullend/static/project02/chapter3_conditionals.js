/*
// function expression gotta be declared before executing the call
const greet = function (name) {
  console.log("Hello there " + name);
};

greet("Joo Hyuk");

speak();

// function declaration. JS compiler defines function first before anything else.
// default value and overide same as Python
// return value also the same.
function speak(content = "whatever") {
  console.log(content);
}

// arrow function

const calcArea = (width, height) => {
  return width * height;
};
// only one parameter, no need parenthesis,
// same for code block
const calcRightArea = (height) => height ** 2;

console.log(calcArea(2, 4));
console.log(calcRightArea(2));
*/
